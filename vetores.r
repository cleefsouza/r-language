# vetores

vetor_1 <- c(1, 2, 3, 4, 5) # atribuindo objetos
vetor_1

vetor_2 <- 0:10 # gerando vetor de 0 a 10
vetor_2

class(vetor_1) # um vetor tem sempre a mesma classe dos objetos que guarda
class(vetor_2)

sub_ex <- vetor_1 - 1 # exemplo de operação básica em todos os objetos
sub_ex

vetor_3 <- as.character(vetor_1) # forçando classes explicitamente
vetor_3

# se o R não entender ele soltará um NA